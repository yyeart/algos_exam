# Билет 3
1. Быстрая сортировка Хоара
2. Красно-черные деревья. Добавление вершины
3. Написать функцию вычисления арифметического выражения, представленного в виде двоичного дерева
4. Найти путь минимальной длины в ориентированном графе, заданном списком ребер, проходящий через заданное ребро
   
# Билет 5
1. Сортировка просеиванием
2. Алгоритмы поиска минимального остового графа
3. Написать операцию вставки в B-дерево
4. Написать все операции для бинарного дерева. Хранение - массив целых чисел

# Билет 7
1. Сортировка Шелла
2. Преобразование инфиксной записи арифметического выражения в постфиксную и вычисление его значения по постфиксной записи
3. Написать функцию поиска значения по заданному целому ключу в B-дереве
4. Написать функцию подсчета в орграфе количества циклов длины равной заданной

# Билет 11
1. Сортировка вставкой
2. AVL-деревья. Балансировка. Вращения
3. Написать функцию удаления из двоичного дерева всех листьев, числовое значение которых отличается от значения родителя более чем на заданное значение
4. Найти самый дешевый путь в ориентированном взвешенном графе, заданном списком ребер, проходящий через данное ребро

# Билет 13
1. Сравнить методы сортировки Шелла и Хоара по затратам времени и памяти, устойчивости, особенностям применения
2. Описать алгоритм преобразования постфиксной записи арифметического выражения в инфиксную
3. Реализовать алгоритм Дейкстры поиска кратчайшего пути
4. Написать функцию добавления пары (ключ-значение) в AVL-дерево, учитывая возможность вращений

# Билет 17
1. Представить связный граф из не менее 7 вершин через матрицу смежности, матрицу инцидентности, список вершин и список ребер. Выполнить топологическую сортировку
2. Алгоритмы нахождения минимального остовного дерева
3. Алгоритм Флойда-Уоршелла
4. Удаление пары ключ значение из AVL с вращениями

# Билет 19
1. Представление двоичного дерева на динамических структурах и на массиве, сравнительный анализ, пример реализации
2. Красно-черные деревья. Алгоритм проверки, является ли дерево красно-черным
3. Написать функции поиска ширины и глубины двоичного дерева
4. Написать функцию добавления пары (ключ-значение) в B-дерево

# Билет 23
1. Сравнить методы сортировки просеиванием и двоичной вставкой по затратам времени и памяти, устойчивости, особенностям приминения.
2. B-дерево. Определение, добавление узла, поиск
3. Написать функцию печати нетерминальных узлов двоичного дерева в порядке обхода ЛКП
4. Написать функцию преобразования арифметического выражения из инфиксной формы в постфиксную

# Билет А
1. Сравнить алгоритмы поиска минимального остова (Прима и Краскала): асимптотика, структуры данных, применимость для разреженных и плотных графов.
2. B-дерево. Операция удаления ключа: случаи заимствования у брата и слияния страниц.
3. Написать функцию проверки, является ли заданное двоичное дерево деревом поиска (BST).
4. Дан ориентированный граф в виде матрицы смежности. Написать функцию, которая находит вершину-«сток» (в неё входят дуги из всех вершин, а из неё не выходит ни одна).

# Билет Б
1. Красно-черные деревья. Операция удаления вершины и исправление нарушений (разбор случаев с «двойным черным» узлом).
2. Алгоритм Флойда-Уоршела: идея динамического программирования, обработка отрицательных весов, обнаружение отрицательных циклов.
3. Бинарное дерево хранится в виде массива целых чисел. Написать функцию, вычисляющую сумму элементов на заданном уровне $L$.
4. Написать функцию, которая по синтаксическому дереву арифметического выражения генерирует его префиксную запись (КЛП).

# Билет В
1. Алгоритм Дейкстры: обоснование, почему не работает на отрицательных весах, и способы оптимизации (матрица vs куча).
2. Сортировка Хоара. Худший случай асимптотики: причины возникновения и методы борьбы (выбор медианы трех, рандомизация).
3. Написать функцию добавления элемента в B-дерево (слайсинг/расщепление переполненного узла).
4. Найти длину кратчайшего пути между двумя вершинами во взвешенном графе с отрицательными ребрами, но без отрицательных циклов (код Флойда-Уоршела).

# Билет Г
1. Способы представления графов (матрицы vs списки). Топологическая сортировка: алгоритм на основе DFS и область применения.
2. AVL-деревья. Оценка высоты. Разбор больших и малых вращений при вставке.
3. Написать функцию, которая находит зеркальное отображение двоичного дерева (меняет местами левого и правого потомка для всех узлов).
4. Написать алгоритм вычисления значения выражения, записанного в постфиксной форме (через стек операндов).